class Title {
  constructor(tconst, titleType, pTitle, oTitle, isAdult, startYear, endYear, runtime, genres){
    this.tconst = tconst
    this.titleType = titleType
    this.pTitle = pTitle
    this.oTitle = oTitle
    this.isAdult = isAdult
    this.startYear = startYear
    this.endYear = endYear
    this.runtime = runtime
    this.genres = genres
  }
}
module.exports = Title;
